import Button from 'react-bootstrap/Button';

function BrowseCardButton() {
    //will need to be passed: onClick, Heading
    
  return (
    <div>
        <Button variant="success">Mark as Read</Button>
    </div>
  )
};

export default BrowseCardButton;